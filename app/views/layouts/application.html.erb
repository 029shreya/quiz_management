<!DOCTYPE html>
<html>
  <head>
    <title>QuizAdmin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="app-body">
    <header class="navbar">
      <div class="navbar-inner">
        <div class="navbar-brand">
          <%= link_to root_path, class: "navbar-logo" do %>
            <span class="navbar-logo-icon">Q</span>
            <span class="navbar-logo-text">Quiz Manager</span>
          <% end %>
        </div>

        <nav class="navbar-links">
          <%= link_to "Quizzes", quizzes_path, class: "nav-link" %>

          <% if user_signed_in? %>
            <span class="nav-text">
              <span class="nav-dot"></span>
              <span class="nav-email"><%= current_user.email %></span>
            </span>

            <%= button_to "Logout",
                  destroy_user_session_path,
                  method: :delete,
                  form: { class: "nav-button-form" },
                  class: "btn btn-outline btn-sm" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "btn btn-outline btn-sm" %>
            <%= link_to "Login", new_user_session_path, class: "btn btn-primary btn-sm" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="page">
      <% flash.each do |key, value| %>
        <div class="flash flash-<%= key %>"><%= value %></div>
      <% end %>

      <div class="page-inner">
        <%= yield %>
      </div>
    </main>

    <footer class="footer">
      <p>© <%= Time.current.year %> Quiz Manager · Built with Ruby on Rails</p>
    </footer>
  </body>
</html>
